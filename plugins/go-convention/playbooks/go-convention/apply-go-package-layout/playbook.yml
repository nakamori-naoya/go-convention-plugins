version: 2
name: apply-go-package-layout
description: 確定済みの論理責務と集約境界を Go の package 配置と import 方向へ写す独立プレイブック
instructions:
  execution:
    directive: 対応する内部skillだけを実行し、他の関心の規約を暗黙に適用しない
requires: []
steps:
  - {id: apply, skill: apply-go-package-layout, purpose: 論理責務を Go の package 配置へ写す, needs: [request], provides: [result]}
