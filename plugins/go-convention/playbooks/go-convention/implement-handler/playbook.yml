version: 2
name: implement-handler
description: Connect-RPC の handler と interceptor を実装する。同名の内部skillへ1工程でルーティングする独立プレイブック
instructions:
  execution:
    directive: 対応する内部skillだけを実行し、他の関心の規約を暗黙に適用しない
requires: []
steps:
  - {id: apply, skill: implement-handler, purpose: Connect-RPC の handler と interceptor を実装する, needs: [request], provides: [result]}
