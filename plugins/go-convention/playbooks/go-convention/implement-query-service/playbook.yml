version: 2
name: implement-query-service
description: CQRS の読み取り側を sqlc で実装する。同名の内部skillへ1工程でルーティングする独立プレイブック
instructions:
  execution:
    directive: 対応する内部skillだけを実行し、他の関心の規約を暗黙に適用しない
requires: []
steps:
  - {id: apply, skill: implement-query-service, purpose: CQRS の読み取り側を sqlc で実装する, needs: [request], provides: [result]}
