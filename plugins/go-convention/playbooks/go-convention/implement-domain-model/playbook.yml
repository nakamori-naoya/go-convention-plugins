version: 2
name: implement-domain-model
description: domain-model 資料から値オブジェクト・エンティティ・集約を Go で実装する。同名の内部skillへ1工程でルーティングする独立プレイブック
instructions:
  execution:
    directive: 対応する内部skillだけを実行し、他の関心の規約を暗黙に適用しない
requires: []
steps:
  - {id: apply, skill: implement-domain-model, purpose: domain-model 資料から値オブジェクト・エンティティ・集約を Go で実装する, needs: [request], provides: [result]}
