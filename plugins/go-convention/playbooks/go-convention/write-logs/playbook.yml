version: 2
name: write-logs
description: log/slog の出し方と出す場所を規約に合わせる。同名の内部skillへ1工程でルーティングする独立プレイブック
instructions:
  execution:
    directive: 対応する内部skillだけを実行し、他の関心の規約を暗黙に適用しない
requires: []
steps:
  - {id: apply, skill: write-logs, purpose: log/slog の出し方と出す場所を規約に合わせる, needs: [request], provides: [result]}
