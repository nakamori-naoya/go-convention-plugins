version: 2
name: test-handler
description: handler のテストを本番同等 DI で書く。同名の内部skillへ1工程でルーティングする独立プレイブック
instructions:
  execution:
    directive: 対応する内部skillだけを実行し、他の関心の規約を暗黙に適用しない
requires: []
steps:
  - {id: apply, skill: test-handler, purpose: handler のテストを本番同等 DI で書く, needs: [request], provides: [result]}
